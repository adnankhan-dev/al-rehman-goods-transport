# Al Rehman Goods Transport

FastAPI application for order, fleet, contractor, plant, site, and transaction management.

## Run

Use `python run.py` from this folder.

## Development

Install dependencies with `pyproject.toml`, then import `create_app` from `al_rehman_goods_transport`.
